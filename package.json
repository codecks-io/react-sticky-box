{
  "private": true,
  "name": "react-sticky-box-workspace",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "description": "Workspace for react-sticky-box",
  "scripts": {
    "lint": "eslint --max-warnings=0 packages *.js",
    "is-prettier": "prettier --list-different 'packages/*/src/**/*.{js,json,css,jsx}' './*.js'",
    "prettier": "prettier --write",
    "prettier:all": "prettier --write 'packages/*/src/**/*.{js,json,css,jsx}' '*.js'",
    "precommit": "lint-staged",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-react-app": "^7.0.0",
    "husky": "^7.0.0",
    "lint-staged": "^12.3.5",
    "prettier": "^2.5.1"
  },
  "lint-staged": {
    "packages/*/src/**/*.{js,json,css,mdx,jsc} *.js": [
      "npm run prettier",
      "npm run lint",
      "git add"
    ]
  }
}
